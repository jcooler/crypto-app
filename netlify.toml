[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — API routes are handled by the function's own `config.path`
# (Netlify Functions 2.0), which takes precedence over redirects.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
